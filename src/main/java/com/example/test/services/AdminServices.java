package com.example.test.services;

import com.example.test.models.Books;
import org.hibernate.StatelessSession;
import org.springframework.stereotype.Service;

//@Service
//public class AdminServices {
//     public void addBook(Books book) {
//         String sql = "INSERT INTO books (title, author, isbn, year_published) VALUES (?, ?, ?, ?)";
//         StatelessSession jdbcTemplate;
//         jdbcTemplate.update(sql, book.getTitle(), book.getAuthor(), book.getIsbn(), book.getYearPublished());
//     }
// }
